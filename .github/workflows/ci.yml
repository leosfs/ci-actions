name: CI

on:
  push:
    branches:
      - develop

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Dependencies
        run: npm install

      - name: Run Tests
        run: npm test

      - name: Build
        run: npm run build

      - name: Deploy to Development
        if: github.ref == 'refs/heads/develop'
        run: |
          # Comandos para implantar no ambiente de desenvolvimento

      - name: Deploy to Production
        if: github.ref == 'refs/heads/master'
        run: |
          # Comandos para implantar no ambiente de produção